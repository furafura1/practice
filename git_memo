git add *
git status
git commit -m "git init"

git e-mail,user_name
git config --global user.email "kakukakudaiou1@gmail.com"
git config --global user.name "furafura1"

git stash "一個前のコードに戻る"

git checkout "commit番号" 'あまり使わないほうが良い'

git log

git remote add origin https://github.com/furafura1/practice.git

ssh-keygen


githubのコマンド
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/furafura1/practice.git
git push -u origin main
